<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- Slice 3.1 提权采集：签名级抓屏许可声明（仅 Shizuku UserService / Root 进程持有，普通进程声明无害） -->
    <uses-permission android:name="android.permission.CAPTURE_VIDEO_OUTPUT" />
    <!-- Slice 3.2 系统内音抓取：签名级内录许可声明（仅 Shizuku UserService / Root 进程持有，普通进程声明无害） -->
    <uses-permission android:name="android.permission.CAPTURE_AUDIO_OUTPUT" />
    <!-- Priv-Bridge-1 Shizuku 提权桥：取值即 rikka.shizuku.ShizukuProvider.PERMISSION 常量
        （历史兼容声明；v13 实际鉴权走 Shizuku.requestPermission 运行时申请 + 用户在管理器侧允许）。 -->
    <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />
    <!-- 后续常驻前台服务（Slice 4.1/6.1）前置：仅声明，暂不启用任何 service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <!-- Fix-FGS-1：常驻改 dataSync 类型，仅需本权限即可前台化（connectedDevice 需 anyOf 蓝牙/网络状态权限，mediaProjection 需授权 token，均不可用，权限声明保留仅作兼容，不再使用对应类型） -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Slice 6.1 常驻前台服务：PARTIAL_WAKE_LOCK（CPU 常醒）+ WiFi 状态读取（局域网 IP / 链路速率） -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- Slice 3.2 前台音频抓取服务类型前置：仅声明，暂不启用任何 service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <uses-sdk tools:overrideLibrary="top.yukonga.miuix.kmp.blur" />

    <application
        android:name=".BlindCastApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:directBootAware="true"
        android:fullBackupContent="@xml/backup_rules"
        android:hasFragileUserData="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.BlindCast">
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.BlindCast"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/filepaths" />
        </provider>

        <!-- Fix-FGS-1 常驻串流前台服务：dataSync 类型（connectedDevice 缺 anyOf 权限致 SecurityException 崩溃循环；mediaProjection 无授权 token 错配，故统一改 dataSync） -->
        <service
            android:name=".core.service.BlindCastForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Priv-Bridge-1 Shizuku 提权桥：Shizuku Binder 下发通道（authorities 必须为 ${applicationId}.shizuku；
            permission/exported/multiprocess 取值按 Shizuku 官方要求，防普通应用访问；UserService 通道无需 <service>
            声明，Shizuku server 按 ComponentName 经 app_process 直接实例化 PrivilegedUserService）。 -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

    </application>

</manifest>
